# 语音停止功能实现说明

## 功能概述
实现按数字键盘"2"键停止当前语音播放功能，并保存中断状态，在下次对话时将中断信息传递给模型。

## 实现细节

### 1. 按键配置
**文件**: `config.py`
- 添加了 `KEY_STOP_VOICE = 'num 2'`
- 在 `get_ui_config()` 方法中将停止按键添加到keys配置中

### 2. StreamingTTSManager 中断控制

**新增成员变量**:
- `self.interrupted`: 是否被中断的标志
- `self.interrupted_text`: 中断时的已处理文本
- `self.interrupted_position`: 中断位置

**新增方法**:
- `interrupt_streaming()`: 中断流式处理
  - 设置中断标志
  - 记录已处理的文本和位置
  - 停止处理循环
  - 清空音频队列
  - 停止当前播放
  - 打印中断信息

- `_clear_audio_queue()`: 清空音频队列中剩余的音频文件
- `_stop_current_playback()`: 停止当前pygame.mixer.music播放
- `get_interrupted_state()`: 获取中断状态
- `reset_interrupt_state()`: 重置中断状态

**修改的方法**:
- `start_streaming()`: 在开始新的流式处理时重置所有中断状态
- `_playback_loop()`: 在播放循环中检查中断标志，及时停止播放
- `_play_audio_file()`: 在pygame播放循环中检查中断标志

### 3. VoiceChatSystem 停止处理

**新增成员变量**:
- `self.should_include_interrupt_context`: 标记是否需要在下次对话中包含中断信息

**新增方法**:
- `_handle_stop_voice()`: 处理停止语音播放
  - 检查是否有正在播放的语音
  - 调用StreamingTTSManager的interrupt_streaming()
  - 获取中断状态并保存到ChatManager
  - 更新系统状态
  - 清理资源
  - 打印提示信息

- `_cleanup_streaming_resources()`: 清理流式资源，重置中断状态

**修改的方法**:
- `_on_llm_content()`: 检查中断标志，如果被中断则忽略后续LLM输出
- `_on_llm_complete()`: 检查中断标志，如果被中断则不对话历史进行更新
- `_chat_with_llm()`: 如果有中断信息，将其添加到用户消息中传递给模型
- `run()`: 在主循环中添加停止按键检测，并在UI提示中显示停止按键说明

### 4. ChatManager 中断信息管理

**新增成员变量**:
- `self.interrupted_conversation`: 保存未完成的对话

**新增方法**:
- `save_interrupted_message()`: 保存中断的对话内容
- `get_interrupted_context()`: 获取中断上下文信息，用于下次对话

### 5. 执行流程

#### 正常流程：
1. 用户说话/输入文字
2. ASR识别
3. 发送给LLM
4. LLM流式输出
5. 实时TTS合成并播放
6. 播放完成，更新对话历史

#### 中断流程：
1. 用户按下"num 2"键
2. StreamingTTSManager.interrupt_streaming()
   - 设置中断标志
   - 记录已处理文本
   - 停止处理循环
   - 清空队列
   - 停止播放
3. VoiceChatSystem._handle_stop_voice()
   - 保存中断内容到ChatManager
   - 标记需要包含中断信息
   - 更新状态
   - 清理资源
4. LLM继续输出（但被忽略）
5. LLM完成后，不更新对话历史
6. 用户下次说话时，中断信息会作为上下文传递给模型

## 关键问题解决

### 1. 线程安全
- 使用 `threading.Event()` 的 `stop_event` 在多个线程间同步中断信号
- 在播放循环和处理循环中都检查中断标志

### 2. 速度差异处理
- TTS合成速度快，语音播放慢
- 中断时立即清空队列，丢弃未播放的音频文件
- 记录的是"已处理文本"而非"已播放文本"

### 3. 模型输出控制
- 无法直接停止模型的流式输出
- 通过设置中断标志，在回调函数中忽略后续输出
- 模型继续输出但不影响系统

### 4. 对话历史保护
- 中断时不在对话历史中添加部分内容
- 只有在正常完成时才添加到历史记录
- 通过 `should_include_interrupt_context` 在下次对话时传递中断信息

## 使用方法

1. 在语音播放过程中按下数字键盘的"2"键
2. 系统会立即停止播放，清空等待播放的音频
3. 打印中断时的文本内容
4. 下次对话时，模型会知道上次对话被中断的位置
5. 可以在对话历史中查看被中断的内容

## 注意事项

1. 中断信息会在下次对话中自动传递给模型
2. 如果不需要传递中断信息，请清空对话历史
3. 中断后模型可能继续输出，但这些输出会被忽略
4. 中断时已合成的音频文件会被清理

