# MCP集成快速检查清单

## 前置条件检查

- [ ] `mcp_client.py` 已复制到项目目录
- [ ] `mcp_manager.py` 已复制到项目目录
- [ ] `mcp_config_example.py` 已复制到项目目录（作为参考）
- [ ] 项目已安装 `requests` 库（HTTP模式需要）
- [ ] 项目已安装 `mcp` 库（stdio模式需要，可选）
- [ ] 了解项目的config.py结构（类方式还是模块方式）

## 集成步骤检查

### 步骤1: config.py配置
- [ ] 打开 `config.py` 文件
- [ ] 添加 `MCP_CONFIG` 配置
- [ ] 配置包含 `enabled` 字段
- [ ] 配置包含 `servers` 列表
- [ ] 至少配置一个MCP服务器（HTTP或stdio）
- [ ] 配置可以被其他模块导入
- [ ] 测试导入：`from config import MCP_CONFIG`

### 步骤2: llm_client.py修改
- [ ] 打开 `llm_client.py` 文件
- [ ] 找到 `LLMClient` 类的 `__init__` 方法
- [ ] 添加 `mcp_manager=None` 参数
- [ ] 添加 `self.mcp_manager = mcp_manager`
- [ ] 找到 `chat_completion` 方法
- [ ] 检查方法是否为 `async`（如果不是，可能需要改为async）
- [ ] 在方法开始处添加工具列表获取逻辑
- [ ] 在LLM调用时传递 `tools` 参数
- [ ] 添加工具调用处理逻辑
- [ ] 添加工具结果处理逻辑
- [ ] 保持现有的流式响应功能
- [ ] 保持现有的性能统计功能
- [ ] 添加错误处理

### 步骤3: voice_chat.py修改
- [ ] 打开 `voice_chat.py` 文件
- [ ] 在文件顶部添加导入：`from mcp_manager import MCPManager`
- [ ] 在文件顶部添加导入：`from config import MCP_CONFIG`（或相应的配置导入）
- [ ] 找到 `VoiceChatSystem` 类的 `__init__` 方法
- [ ] 添加MCP管理器初始化代码
- [ ] 修改LLM客户端初始化，传递 `mcp_manager` 参数
- [ ] 添加 `_initialize_mcp` 异步方法
- [ ] 找到 `run` 方法（或主循环方法）
- [ ] 在方法开始处调用 `await self._initialize_mcp()`
- [ ] 添加错误处理

## 代码检查点

### config.py检查
```python
# 检查点1: 配置格式正确
MCP_CONFIG = {
    "enabled": True,
    "servers": [...]
}

# 检查点2: 可以导入
from config import MCP_CONFIG
```

### llm_client.py检查
```python
# 检查点1: 添加了mcp_manager参数
def __init__(self, ..., mcp_manager=None):
    self.mcp_manager = mcp_manager

# 检查点2: 获取工具列表
tools = None
if self.mcp_manager:
    tools = self.mcp_manager.get_tools_openai_format()

# 检查点3: 传递工具给LLM
response = client.chat.completions.create(
    ...,
    tools=tools,
    tool_choice="auto"
)

# 检查点4: 处理工具调用
if tool_calls:
    # 执行工具调用
    # 添加工具结果
    # 继续生成回复
```

### voice_chat.py检查
```python
# 检查点1: 导入模块
from mcp_manager import MCPManager
from config import MCP_CONFIG

# 检查点2: 初始化MCP管理器
self.mcp_manager = None
if MCP_CONFIG.get("enabled"):
    servers_config = MCP_CONFIG.get("servers", [])
    if servers_config:
        self.mcp_manager = MCPManager(servers_config)

# 检查点3: 传递给LLM客户端
self.llm_client = LLMClient(
    ...,
    mcp_manager=self.mcp_manager
)

# 检查点4: 异步初始化
async def _initialize_mcp(self):
    if self.mcp_manager:
        await self.mcp_manager.connect_all()

# 检查点5: 在run方法中调用
async def run(self):
    await self._initialize_mcp()
    # ... 其他代码
```

## 测试检查

### 基础测试
- [ ] 测试MCP配置加载
- [ ] 测试MCP管理器初始化
- [ ] 测试MCP服务器连接
- [ ] 测试工具列表获取

### 功能测试
- [ ] 测试工具调用
- [ ] 测试LLM使用工具
- [ ] 测试完整的对话流程
- [ ] 测试流式响应（如果支持）

### 错误测试
- [ ] 测试MCP未启用的情况
- [ ] 测试MCP连接失败的情况
- [ ] 测试工具调用失败的情况
- [ ] 测试MCP服务器不可用的情况

## 常见问题检查

### 问题1: 导入错误
- [ ] 检查文件路径是否正确
- [ ] 检查模块名称是否正确
- [ ] 检查Python路径配置

### 问题2: 异步问题
- [ ] 检查方法是否为async
- [ ] 检查是否在异步环境中调用
- [ ] 检查事件循环是否正确

### 问题3: 配置问题
- [ ] 检查配置格式是否正确
- [ ] 检查配置是否可以导入
- [ ] 检查配置值是否正确

### 问题4: 工具调用问题
- [ ] 检查工具列表是否为空
- [ ] 检查工具名称是否正确
- [ ] 检查工具参数是否正确
- [ ] 检查LLM是否支持工具调用

## 完成检查

- [ ] 所有步骤已完成
- [ ] 代码可以正常运行
- [ ] 测试通过
- [ ] 错误处理完善
- [ ] 文档已更新（如果需要）

## 下一步

- [ ] 进行实际测试
- [ ] 优化性能
- [ ] 添加日志
- [ ] 添加监控
- [ ] 更新文档

