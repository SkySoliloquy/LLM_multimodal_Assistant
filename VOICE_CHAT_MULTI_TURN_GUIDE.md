# 语音对话系统 - 多轮对话功能指南

## 🎯 功能概述

`voice_chat.py`现已升级为支持多轮对话的语音交互系统，结合了语音识别、文字输入和对话历史管理功能。

## 🚀 新增功能

### 1. 多轮对话支持
- **对话历史管理**: 自动保存用户和助手的对话历史
- **上下文理解**: LLM能够理解之前的对话内容
- **历史限制**: 自动管理对话历史长度，避免内存溢出

### 2. 多种输入方式
- **语音输入**: 按`z`键录音，松开停止
- **文字输入**: 按`t`键进入文字输入模式
- **混合使用**: 可以语音和文字交替使用

### 3. 对话管理功能
- **查看历史**: 按`h`键查看对话历史
- **清空历史**: 按`c`键清空对话历史
- **历史统计**: 显示当前对话轮次

## ⌨️ 操作指南

### 基本操作
```
🎤 按 'z' 键开始录音，松开停止
⌨️  按 't' 键输入文字对话
📜 按 'h' 键查看对话历史
🗑️  按 'c' 键清空对话历史
❌ 按 'q' 键退出程序
```

### 详细操作说明

#### 1. 语音输入
1. 按住`z`键开始录音
2. 对着麦克风说话
3. 松开`z`键停止录音
4. 系统自动进行语音识别和LLM回复

#### 2. 文字输入
1. 按`t`键进入文字输入模式
2. 在命令行输入文字消息
3. 按回车发送消息
4. 系统自动调用LLM并回复

#### 3. 对话历史管理
- **查看历史**: 按`h`键查看最近的对话记录
- **清空历史**: 按`c`键清空所有对话历史
- **历史统计**: 在每次LLM回复后显示当前对话轮次

## 🔧 技术实现

### 核心组件

#### 1. ChatManager类
```python
class ChatManager:
    def __init__(self, system_prompt, max_history=10):
        # 初始化对话管理器
        self.system_prompt = system_prompt
        self.max_history = max_history  # 最大保留10轮对话
        self.history = []
```

**主要功能**:
- 管理对话历史
- 自动修剪历史长度
- 提供完整的消息列表给LLM

#### 2. 多轮对话流程
```python
def chat_with_llm(self, user_message):
    # 1. 添加用户消息到历史
    self.chat_manager.add_user_message(user_message)
    
    # 2. 使用完整历史调用LLM
    response = self.client.chat.completions.create(
        model="DeepSeek-V3-Fast:5HpV9XORBHupqPOgslPz2X",
        messages=self.chat_manager.get_messages(),  # 包含历史
        stream=True
    )
    
    # 3. 添加助手回复到历史
    self.chat_manager.add_assistant_message(full_content)
```

### 历史管理机制

#### 自动修剪
- 默认保留最近10轮对话（20条消息）
- 超出限制时自动删除最早的对话
- 始终保持系统提示词在消息列表首位

#### 消息格式
```python
messages = [
    {"role": "system", "content": "系统提示词"},
    {"role": "user", "content": "用户消息1"},
    {"role": "assistant", "content": "助手回复1"},
    {"role": "user", "content": "用户消息2"},
    {"role": "assistant", "content": "助手回复2"},
    # ... 更多对话历史
]
```

## 📊 性能优化

### 1. 历史长度控制
- **默认设置**: 保留10轮对话（20条消息）
- **内存优化**: 自动修剪历史，避免内存溢出
- **可配置**: 可通过修改`max_history`参数调整

### 2. 流式输出
- **实时响应**: LLM回复采用流式输出
- **性能指标**: 显示首Token时间、生成速度等
- **用户体验**: 逐字符显示，响应更自然

### 3. 错误处理
- **录音失败**: 自动检测并提示
- **识别失败**: 显示具体错误信息
- **网络异常**: 优雅处理API调用失败

## 🎮 使用示例

### 示例对话流程
```
🎤 语音对话系统 - 多轮对话版本
============================================================
🎤 按 'z' 键开始录音，松开停止
⌨️  按 't' 键输入文字对话
📜 按 'h' 键查看对话历史
🗑️  按 'c' 键清空对话历史
❌ 按 'q' 键退出程序
============================================================

🎤 开始录音... (松开z键停止)
⏹️ 录音结束
📊 录音时长: 2.34秒
🔄 正在转录音频...
✅ 识别结果: 你好，请介绍一下Python
🤖 正在生成回复...

助手: 你好！Python是一种高级编程语言...

--------------------------------------------------
首Token时间 (TTFT): 0.234秒
总字符数: 156
字符速度: 45.2 tokens/秒
LLM总耗时: 3.456秒
对话轮次: 1
--------------------------------------------------

按 't' 键进入文字输入模式
📝 用户输入: 能给我一个Python代码示例吗？
🤖 正在生成回复...

助手: 当然！这里是一个简单的Python示例...

--------------------------------------------------
对话轮次: 2
--------------------------------------------------
```

### 历史查看示例
```
==================================================
对话历史:
==================================================
1. [用户]: 你好，请介绍一下Python
2. [助手]: 你好！Python是一种高级编程语言，由Guido van Rossum于1991年创建...
3. [用户]: 能给我一个Python代码示例吗？
4. [助手]: 当然！这里是一个简单的Python示例...
==================================================
```

## 🔍 故障排除

### 常见问题

#### 1. 对话历史丢失
**问题**: 重启程序后对话历史丢失
**解决**: 这是正常现象，历史保存在内存中。如需持久化，可以添加文件保存功能。

#### 2. 历史过长导致响应慢
**问题**: 对话轮次太多，LLM响应变慢
**解决**: 按`c`键清空历史，或调整`max_history`参数。

#### 3. 混合输入混乱
**问题**: 语音和文字输入混用时感觉混乱
**解决**: 这是正常现象，两种输入方式会共享同一个对话历史。

### 性能优化建议

1. **合理使用历史**: 定期清空不必要的对话历史
2. **网络优化**: 确保网络连接稳定
3. **硬件配置**: 使用GPU加速语音识别

## 🎯 最佳实践

### 1. 对话管理
- 定期查看对话历史，了解对话进度
- 在开始新话题时清空历史
- 利用历史进行深入讨论

### 2. 输入方式选择
- **语音输入**: 适合快速表达想法
- **文字输入**: 适合精确表达或复杂问题
- **混合使用**: 根据场景灵活切换

### 3. 系统提示词优化
- 在`system_content.txt`中设置合适的角色和风格
- 考虑多轮对话的特点调整提示词
- 定期更新提示词以适应需求

---

**现在你的语音对话系统支持完整的多轮对话功能！可以像聊天一样与AI进行连续对话。**
