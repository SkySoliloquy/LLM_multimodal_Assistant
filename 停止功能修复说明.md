# 停止功能修复说明

## 发现的问题
1. **主线程阻塞**：LLM 的流式输出在主线程中阻塞执行，导致按键检测无法及时响应
2. **音频停止不彻底**：虽然设置了中断标志，但pygame播放没有立即停止

## 修复内容

### 1. LLM调用改为后台线程（voice_chat.py:239-275）
```python
def _chat_with_llm(self, user_message: str):
    # ... 
    # 在后台线程中调用LLM，避免阻塞主线程
    def call_llm():
        result = self.llm_client.chat_completion(
            messages=self.chat_manager.get_messages(),
            stream=True,
            on_content=self._on_llm_content if self.streaming_tts_enabled and self.tts_enabled else None,
            on_complete=self._on_llm_complete
        )
        # ...
    
    # 启动后台线程执行LLM调用
    import threading
    llm_thread = threading.Thread(target=call_llm, daemon=True)
    llm_thread.start()
    
    # 不等待线程完成，让主线程继续响应按键
```

**效果**：主线程不再被LLM调用阻塞，可以及时响应按键

### 2. 强化音频停止（streaming_tts_manager.py:468-480）
```python
def _stop_current_playback(self):
    """停止当前播放"""
    try:
        import pygame
        # 强制停止pygame mixer
        pygame.mixer.pause()
        pygame.mixer.music.stop()
        pygame.mixer.quit()
        # 重新初始化以便后续可以继续播放
        pygame.mixer.init()
        print("🛑 已强制停止当前音频播放")
    except Exception as e:
        print(f"⚠️ 停止播放时出错: {e}")
```

**效果**：完全停止pygame播放，不留任何残留

### 3. 优化中断处理顺序（streaming_tts_manager.py:437-467）
```python
def interrupt_streaming(self):
    """中断流式处理"""
    # 无论is_processing状态如何，都允许中断
    if self.interrupted:
        return  # 已经中断过了

    print("🛑 中断流式TTS处理...")
    
    # 设置中断标志（必须在最前面）
    self.interrupted = True
    
    # 停止当前播放的音频（优先执行）
    self._stop_current_playback()
    
    # 清空等待播放的音频队列
    self._clear_audio_queue()
    # ...
```

**效果**：立即设置中断标志，优先停止播放

### 4. 播放前检查中断（streaming_tts_manager.py:375-404）
```python
def _play_audio_file(self, audio_path: str):
    """播放音频文件"""
    try:
        import pygame
        
        # 如果已经被中断，跳过播放
        if self.interrupted:
            print("⚠️ 音频已被中断，跳过播放")
            return
        
        # 播放过程中也检查中断
        while pygame.mixer.music.get_busy():
            if self.interrupted:
                pygame.mixer.music.stop()
                pygame.mixer.music.unload()
                print("🛑 播放被用户中断")
                return
            time.sleep(0.1)
```

**效果**：在播放的每个阶段都检查中断状态

## 完整执行流程

### 正常流程
1. 用户说话/输入
2. ASR识别
3. 后台线程调用LLM
4. LLM输出 → `_on_llm_content` → 检查中断 → TTS合成 → 播放
5. 播放完成

### 中断流程（按下停止键）
1. 主线程检测按键 → 立即调用 `_handle_stop_voice()`
2. `interrupt_streaming()` 立即：
   - 设置 `interrupted = True`
   - 强制停止pygame播放
   - 清空音频队列
3. 后续LLM输出被 `_on_llm_content` 中的检查忽略
4. 播放循环检测到中断，立即退出
5. 保存中断状态

## 关键改进

### ✅ 主线程不再阻塞
- LLM调用在后台线程执行
- 按键检测能够及时响应

### ✅ 立即停止播放
- 强制停止pygame（pause + stop + quit + init）
- 清空音频队列
- 在多个位置检查中断标志

### ✅ 忽略后续输出
- `_on_llm_content` 检查 `interrupted` 标志
- 即使LLM继续输出也不处理

## 测试要点

1. 按下停止键应该立即停止播放
2. 不应该等待LLM输出完成
3. 播放的音频立即停止
4. 队列中的音频被清空
5. 下次对话会包含中断信息

